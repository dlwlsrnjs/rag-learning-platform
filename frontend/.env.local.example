# Optional: if you want the frontend to preset an OpenAI key, copy this file
# to .env.local. The key is sent from the browser to the backend on each
# /api/pipelines/run request — it is NEVER persisted server-side.
NEXT_PUBLIC_DEFAULT_OPENAI_KEY=
